spring.application.name=blog-pessoal
# Configura��o da conex�o com o banco de dados PostgreSQL 12
spring.datasource.url=jdbc:postgresql://localhost:5432/blog_pessoal
spring.datasource.username=blog
spring.datasource.password=blog
spring.datasource.driver-class-name=org.postgresql.Driver
# Dialeto do Hibernate para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# Estrat�gia de cria��o do schema (opcional)
spring.jpa.hibernate.ddl-auto=update
# Mostrar queries no console (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
api.security.token.secret=${JWT_SECRET:12345678}